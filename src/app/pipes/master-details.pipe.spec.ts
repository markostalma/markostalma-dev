import { MasterDetailsPipe } from './master-details.pipe';

describe('MasterDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new MasterDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
